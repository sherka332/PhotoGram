# PhotoGram 2.0 Complete

Instagram-style Telegram Mini App foundation: feed, profiles, search, posts, likes, comments, saves, follows, reports, notifications, direct messages and admin API.

Set `ADMIN_IDS` to your Telegram numeric ID before deployment. SQLite is included for simple testing; use persistent storage/database for production.
